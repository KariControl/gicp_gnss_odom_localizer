# Pure localization evaluation profiles

This data-only package installs recording-specific evaluation overrides and
dataset manifests. Keeping them separate prevents accepted or experimental
tuning from being mistaken for generic bringup defaults while preserving the
repository/install/artifact SHA-256 provenance checks used for publication.

The profiles are not universal sensor defaults. Reuse them only for the exact
recording and sensor setup documented by each profile README.
